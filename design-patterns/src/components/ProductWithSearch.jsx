import React from "react";

const ProductWithSearch = (ProductList) => {
  return (
    <div>
      const product = []; return <ProductList product={product} />;
    </div>
  );
};

export default ProductWithSearch;
