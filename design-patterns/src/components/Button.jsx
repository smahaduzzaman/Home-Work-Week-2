import React from "react";
import withAuth from "./../pages/Profile";

export const ButtonWithColor = (Button) => {
  return <Button></Button>;
};

const Button = () => {
  return (
    <div>
      <h2>This is Button</h2>
    </div>
  );
};

export default Button;
